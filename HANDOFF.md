# Handoff

Session-to-session working state for AI Field Filler. Read this and `PITFALLS.md` on spawn.
Update it at milestones; keep it current to reality (`git log` is the authoritative history).

## Current state

Stable on `main`. Recent work: OpenRouter streaming speech synthesis, a full model-capability
audit across all four providers, and Developer Tools becoming configurable/abortable. See
`git log` and `PITFALLS.md` for the details behind those changes.

Claude Code onboarding was just added: root + nested `CLAUDE.md`, `.claude/rules/`,
`.claude/commands/`, vendored `.claude/skills/`, and this file.

## In flight

_Nothing in flight. Add work-in-progress here as it starts._

## Next steps

_None queued. Add ordered, independently-verifiable next tasks here at session close._
